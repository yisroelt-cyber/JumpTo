// src/services/rowHeightTrace.js
// Diagnostics removed (no-op stubs retained to avoid churn in import graphs during incremental cleanup).
// If you reintroduce diagnostics later, restore the prior implementation from history.

/**
 * Diagnostics stub: previously appended trace entries for RowHeightPreset persistence.
 * @returns {Promise<void>}
 */
export function diagTraceRowHeight() {
  return Promise.resolve();
}

/**
 * Diagnostics stub: previously flushed accumulated trace log to the diagnostics sheet.
 * @returns {Promise<void>}
 */
export function diagFlushRowHeightTrace() {
  return Promise.resolve();
}
